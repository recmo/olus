a_call “intrinsics”

a_def par0 par1: a_call arg0 arg1

b_call arg0 arg1
    with_block_def par0 par1: b_call arg0 arg1

fib n return:
    if (isZero n) (:return 0) (:sub n 1 (m:))
    if (isZero m) (:return 1) (:sub m 1 (k:))
    return (add (fib m) (fib k))
